docker ps -a - все контейнеры

docker-compose stop - остановить
docker-compose down - остановить и удалить

docker-compose down  && docker-compose up -d --build - пересобрать все заново

docker-compose down
docker-compose build --no-cache app  # Пересобираем с новыми настройками
docker-compose up -d

docker exec -it symfony-dockerlocal-nginx-1 sh - провалиться внутрь контейнера
cat /var/log/nginx/error.log - посмотреть логи nginx

docker exec -it  symfony-dockerlocal-app-1 cat /etc/os-release - проверить OS

apk add --no-cache nano - для nano в symfony-dockerlocal-nginx-1


====
mkdir -p docker/xdebug/logs
chmod -R 777 docker/xdebug/logs
